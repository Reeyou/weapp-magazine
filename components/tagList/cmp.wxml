<!--标签列表-->
<view class='container'>
  <slot name='add'></slot>
  <c-tag
    class='tag'
    wx:for="{{tagList}}"
    wx:key="{{type}}"
    tag="{{item.tag}}"
    tagId="{{item.tagId}}"/>
  <slot name='more'></slot>
</view>


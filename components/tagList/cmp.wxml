<!--标签列表-->
<view class='container'>
  <slot name='add'></slot>
  <c-tag
    class='tag'
    wx:for="{{tagList}}"
    wx:key="{{type}}"
    tag="{{item.type}}"
    tagId="{{item.typeId}}"/>
  <slot name='more'></slot>
</view>

